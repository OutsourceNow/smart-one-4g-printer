repositories{    
 jcenter()
   flatDir {
     dirs 'libs'
   }
}

dependencies {
  //compile (name:'onelib', ext:'aar')
  implementation 'androidx.appcompat:appcompat:1.2.0'
  implementation(name: 'onelib', ext: 'aar') //one_3G
}

android {
 packagingOptions {
  exclude 'META-INF/NOTICE'
  exclude 'META-INF/LICENSE'
 }
}